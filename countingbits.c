/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* countBits(int n, int* returnSize) {
    for (int i =0; i<=n; i++){
        
        //binary conversion
        int bin;
        while (n==1 || n==0){
           
           ; 
        }
        
        


    }
}